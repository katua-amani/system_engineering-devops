#!/usr/bin/env bash
# This script lists all files in the current directory, displaying only the part after the first dash

for file in *; do
    # Skip hidden files
    [[ "$file" == .* ]] && continue
    # Extract and display the part after the first dash
    echo "${file#*-}"
done
